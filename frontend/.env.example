# Leave empty for same-origin local requests. A hosted static build must use the
# exact credential-free HTTPS backend origin, without a path, query, or fragment.
# The Render Static Site build receives this value from the backend service's
# RENDER_EXTERNAL_URL; do not commit or guess the future provider hostname.
VITE_API_BASE_URL=

# Ordinary API timeout in milliseconds. Local development defaults to 10000.
# The Render Free Blueprint sets this to 90000 to cover a bounded cold start.
VITE_API_TIMEOUT_MS=10000
